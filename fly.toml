# ZÆRØ BOT - Configuração Fly.io
app = "zaero-bot"
primary_region = "gru"

[build]

[env]
  NODE_ENV = "production"
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = "30s"
    timeout = "10s"
    grace_period = "60s"
    method = "GET"
    path = "/"

[[vm]]
  memory_mb = 512
  cpu_kind = "shared"
  cpus = 1
