services:
  - type: web
    name: zaero-bot
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm run web
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /
    autoDeploy: true
